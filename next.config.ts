/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
//x  experimental: { appDir: true },
  //basePath: '/agent',          // tells Next.js all routes start with /agent
  //assetPrefix: '/agent',       // tells Next.js to prefix _next/static URLs
}

module.exports = nextConfig;
